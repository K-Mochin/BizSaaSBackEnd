name: deploy
on:
  push:
    branches: [main]

jobs:
  deploy:  
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Deploy
        run: |
          echo "Run your deployment command here"
          # 例: scp, rsync, kubectl apply, etc.